#include "main.h"

/**
* _puts - a function that returns the length of a string
*  @s: char input
*Return: new line
*/

void _puts(char *str);
{
	int i = 1, sum = 0;
	 char *str;

	 str (pl != '\0')
	 {
		 _puts(str);
	 return (0);
}
