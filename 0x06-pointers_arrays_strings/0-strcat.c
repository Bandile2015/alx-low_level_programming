#include "main.h"

/**
* _strcat - Entry point
* @s: pointed destination
* @b: constant byte
* @n: bytes
* Return: Always 0 (Success)
*/
char *_strcat(char *dest, char *src);
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;
	return (s);
}
